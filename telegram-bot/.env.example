# Скопируй в telegram-bot/.env и заполни SUPABASE_SERVICE_ROLE_KEY из Supabase Dashboard → Settings → API.

# Токен бота (тот же, что VITE_TELEGRAM_BOT_ID во frontend/.env)
TELEGRAM_BOT_TOKEN=...

# URL проекта (тот же, что VITE_SUPABASE_URL во frontend/.env)
SUPABASE_URL=https://rpqnvzfyqrtsjtpwhuse.supabase.co

# Ключ service_role из Supabase Dashboard → Settings → API (не anon key!)
SUPABASE_SERVICE_ROLE_KEY=...

# На локалке ставь false, чтобы не конфликтовать с ботом на Render (409). На Render не задавай или true.
# ENABLE_TELEGRAM_POLLING=false
