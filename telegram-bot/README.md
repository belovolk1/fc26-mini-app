# FC Area — Telegram Bot

Бот для связи соперников в Mini App, когда у соперника нет username в Telegram.

## Что делает

При переходе по ссылке `https://t.me/ВАШ_БОТ?start=contact_<telegram_id>` бот отправляет сообщение с кнопкой «💬 Написать сопернику». По нажатию открывается чат с этим пользователем в приложении Telegram.

## Запуск

Бот подхватывает токен и имя бота из **`frontend/.env`** (если там уже есть `VITE_TELEGRAM_BOT_ID` и `VITE_TELEGRAM_BOT_USERNAME`, отдельный файл не нужен).

1. В папке `telegram-bot`:
   ```bash
   npm install
   node index.js
   ```
2. Токен берётся в таком порядке:
   - `BOT_TOKEN` из `telegram-bot/.env` или из окружения;
   - если нет — `VITE_TELEGRAM_BOT_ID` из `frontend/.env`.
3. Имя бота для ссылок задаётся во фронте в `frontend/.env`: `VITE_TELEGRAM_BOT_USERNAME=...`.

## Деплой

На сервере задайте `BOT_TOKEN` (или `VITE_TELEGRAM_BOT_ID`) в переменных окружения. Для продакшена удобно использовать `pm2` или systemd.
